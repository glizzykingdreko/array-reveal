{
  "name": "array-reveal-web",
  "version": "1.0.0",
  "description": "Web interface for array-reveal deobfuscator",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "@babel/core": "^7.x.x",
    "babel-plugin-array-reveal": "../babel-plugin-array-reveal"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 